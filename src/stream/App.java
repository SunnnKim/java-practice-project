package stream;

public class App {
    // Filter
    // 걸러내기
    // Map
    // generate
    // limit
    // anyMatch
    //

/*
    stream api

    1. filter
        - 걸러내기
        - 스트림 파이프라인으로 걸러내는 건데 특정한 타입이 지나감
        - 여기서는 OnlineClass 라는 타입의 인스턴스가 지나가고, 거기에서 타이틀이 spring으로 시작하는 걸 걸러냄 → filter 라는 오퍼레이션이 작동
        - 오퍼레이션에 들어오는 타입이 어떤 인자인지 중요함
    2. map
        - 들어오는 타입→ 나가는 타입(다른 타입으로 변경)
        - 중개형 오퍼레이터, stream 반환
        - 메서드 레퍼런스로 바꿀 수 있음

    3. flatMap
        - 리스트를 flatten 시킨다 → 납작하게 만든다
        - 안에 있는것들을 다 꺼내서 스트림으로 만든다
        - 리스트 타입으로 들어옴
 */
}
